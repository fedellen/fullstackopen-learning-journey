import AsyncStorage from '@react-native-community/async-storage';

class AuthStorage {
  constructor(namespace = 'auth') {
    this.namespace = namespacel;
  }

  getAcessToken() {}

  setAccessToken(accessToken) {}

  removeAccessToken() {}
}

export default AuthStorage;
